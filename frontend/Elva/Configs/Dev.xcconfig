// Dev-окружение (Debug-сборки).
// Debug ходит на тот же прод-сервер, что и Release — локальный бэкенд не нужен.
// Для возврата на локальный бэк: http/ws + localhost:8080 (симулятор) или LAN-IP (./setip.sh).

API_SCHEME = https
WS_SCHEME = wss

API_HOST = api.event-serv.ru
